/** Barrel — `import { ... } from "@/lib/api"` for types, the client, and every endpoint function. */
export * from "@/lib/api/types";
export * from "@/lib/api/client";
export * from "@/lib/api/endpoints";
